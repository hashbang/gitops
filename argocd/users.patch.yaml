apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  # disable admin account
  accounts.admin.enabled: "false"

  # admins
  accounts.benharri: apiKey,login
  accounts.daurnimator: apiKey,login
  accounts.dpflug: apiKey,login
  accounts.drgrove: apiKey,login
  accounts.kellerfuchs: apiKey,login
  accounts.lrvick: apiKey,login
  accounts.ryan: apiKey,login
