# See pgsql_table(5)

domain = hashbang.sh
# TODO(daurnimator): DNS entry here had to be manually resolved
hosts = postgresql://mail:userdb-mail-lookup@104.248.21.126:25060/userdb?sslmode=require
dbname = userdb
query = select name || '@' || host from passwd where name = '%u'
